/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'via.placeholder.com',
          'images.unsplash.com',
          'i.pinimg.com',
          'avatars.githubusercontent.com'
        ],
      },
};

export default nextConfig;
